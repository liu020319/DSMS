<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.medicine.mapper.PrescriptionMapper">

    <select id="selectPageWithRealName" resultType="com.medicine.entity.Prescription">
        SELECT p.* FROM prescription p
        LEFT JOIN sys_user u ON p.user_id = u.user_id AND u.deleted = 0
        WHERE p.deleted = 0
        <if test="userId != null">
            AND p.user_id = #{userId}
        </if>
        <if test="medicineId != null">
            AND p.medicine_id = #{medicineId}
        </if>
        <if test="realName != null and realName != ''">
            AND u.real_name LIKE CONCAT('%', #{realName}, '%')
        </if>
        ORDER BY p.create_time DESC
    </select>

</mapper>
